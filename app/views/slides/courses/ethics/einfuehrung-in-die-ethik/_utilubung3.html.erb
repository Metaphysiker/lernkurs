<h3 class="text-center">Prinzip?</h3>
<hr />
<p>
  Welches Element des Utilitarismus <b>passt nicht</b> zur folgenden Aussage?
</p>
<p>
  <em>
    "Jeder ist auf sich alleine gestellt.
    Daher ist es in Ordnung,
    wenn jeder nur seine eigenen Ziele verfolgt
    oder nur das macht, was zur Befriedigung seiner eigenen Interessen oder Bedürfnissen dient.
    Eine Handlung ist somit gut, wenn ihre Konsequenzen zur Erfüllung deiner Ziele dienen."
  </em>
</p>
<p>
  <button type="button" class="btn btn-primary multilinebutton btn-block">Der Konsequentialismus</button>
  <button type="button" class="btn btn-primary multilinebutton btn-block">Das Nutzenprinzp</button>
  <button type="button" class="btn btn-primary multilinebutton btn-block">Das Universalprinzp</button>
</p>


<script>
  $( document ).ready(function() {
    console.log("utilubung2 loaded!");
    console.log("value:");
    console.log($('input[name="praeferenz"]').val());

    var points = 100;
    var score = 0;

    $( "#utilubung" ).click(function() {

      if (['glück', 'glücks', 'glueck', 'gluecks'].indexOf($('input[name="glueck"]').val().toLowerCase()) >= 0) {
        $('input[name="glueck"]').removeClass("is-invalid");
        $('input[name="glueck"]').addClass("is-valid");
      } else {
        $('input[name="glueck"]').removeClass("is-valid");
        $('input[name="glueck"]').addClass("is-invalid");
        points = points - 50;
      }

      if (['präferenz', 'praeferenz'].indexOf($('input[name="praeferenz"]').val().toLowerCase()) >= 0) {
          $('input[name="praeferenz"]').removeClass("is-invalid");
          $('input[name="praeferenz"]').addClass("is-valid");
      } else {
        $('input[name="praeferenz"]').removeClass("is-valid");
        $('input[name="praeferenz"]').addClass("is-invalid");
        points = points - 50;
      }

    });

  });
</script>
