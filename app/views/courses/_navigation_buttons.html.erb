<div class="text-center">
  <button type="button" class="btn btn-info get-slide-button <%= "disabled" if current_slide <= 0 %> mx-2" slide="<%= current_slide - 1 %>">Zurück</button>
  <%= current_slide + 1 %> / <%= course.slides.count %>
  <button type="button" class="btn btn-info get-slide-button <%= "disabled" if current_slide == course.slides.count-1 %> mx-2" slide="<%= current_slide + 1 %>">Weiter</button>
</div>

<script>
  $(document).on('ready turbolinks:load', function() {
    console.log("Buttons delivered!");
  });
</script>
